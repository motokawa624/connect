
		<table class="sidebar">
			<tr class="sidebar-content">
				<th><%= attachment_image_tag(user, :profile_image, :fill, 100, 100, fallback: "no_image.jpg") %></th>
			</tr>

			<tr>
				<th class="sidebar-content">名前：<%= current_user.name %></th>
			</tr>
				<th class="sidebar-content"><%= link_to "#{user.follower.count}フォロー", user_follows_path(user) %></th>
			</tr>

			<tr>
				<th class="sidebar-content"><%= link_to "#{user.followed.count}フォロワー", user_followers_path(user)%></th>
			</tr>

			<tr>
				<th class="sidebar-content"><%= link_to "マイページ", user_path(current_user), class: "btn btn-primary" %></th>
			</tr>

			<tr>
				<th class="sidebar-content"><%= link_to "マイチーム", home_path(user_id: current_user.id), class: "btn btn-primary" %></th>
			</tr>
		</table>
